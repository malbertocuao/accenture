server:
  port: 8010

spring:
  application:
    name: "accounts"

  datasource:
    url: jdbc:h2:mem:accountsdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ''

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true

  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"

  # Configuración de perfiles por config server
  profiles:
    active: "prod"
  config:
    import: "optional:configserver:http://localhost:8071"

#  Configuración de perfiles por archivo
#  config:
#    import:
#      - "application_qa.yml"
#      - "application_prod.yml"
#  profiles:
#    active:
#      - "default"

management:
  endpoints:
    web:
      exposure:
        include: "*"